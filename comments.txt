1. При попытке заполнения таблицы staging.user_order_log через COPY ( #row_count = engine.execute(f'COPY {pg_schema}.{pg_table} FROM PROGRAM \'curl {s3_filename}\' DELIMITER \',\' CSV HEADER ')
    #print(f'{row_count} rows was inserted') получаю ошибку, поскольку в csv-файле на один столбец больше. Решений кроме как добавление еще столбца я не нашел, поэтому вернулся к варианту через pandas
Но вариант заполнения через запрос мне очень понравился:)
2. Не очень понял про '...ON CONFLICT ... DO UPDATE SET". В конфликте можно указать либо столбец либо вид конфликта? Например, хочу, чтобы при возникновении конфликтов обновлялись все столбцы, но Postgresql
 требует указания столбца. Не очень понимаю как решить данную проблему, избежав указания столбца, указывать CONSTRAINT?
3. ТАкже вопрос, вот делаю ON CONFLICT (id) DO UPDADE SET id= <>. В <> необходимо выполнить запрос, а то указания столбца по алиасу приводи к ошибке?

